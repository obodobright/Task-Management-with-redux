import styled from "styled-components";

export const TodoNav = () => {
  return (
    <Container>
      <WrapperText>MY TODO</WrapperText>
    </Container>
  );
};

const WrapperText = styled.div`
  padding: 30px;
  font-size: 30px;
`;
const Container = styled.div``;
